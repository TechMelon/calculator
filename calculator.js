let res = 0;
// document.getElementsByClassName("screen").textContent = res
// const screenElement = document.querySelector('.screen');
// screenElement.textContent = res;
document.querySelector(".screen").textContent = res;

function set(Number) {
        const screenElement = document.querySelector('.screen');
        screenElement.textContent = Number;
    }
